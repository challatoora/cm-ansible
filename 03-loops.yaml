- name: installing sf using loops
  hosts: local
  connection: local
  become: yes

  tasks:
  - name: isbtalling ngix
    ansible.builtin.package:
      name: "{{ item }}"
    loop:
    - nginx
    - mysql

  - name: satrted the sertver
    ansible.builtin.service:
      name: nginx
      state: started
      