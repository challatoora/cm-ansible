- name: variables
  hosts: local
  connection: local

  vars:
   name: murali 
   training: devscepos 
  task:
    - name: printing variables
      ansible.builtin.debug:
        msg: "iam {{ name }} learining {{training}}"
