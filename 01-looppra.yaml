- name : installing packages
  hosts: local
  connection: local
  become: yes

  tasks:
  - name : installing packages
    ansible.builtin.package:
    #   name: "{{ item }}"
    #   state: present
    # loop:
    # - nginx
    # - mysql
    # - curl
    # - git
    # - wget
      name: "{{ item.name }}"
      state: "{{ item.state }}"

    loop:
    - { name : mysql, state : absent }
    - { name : tree, state : present }

