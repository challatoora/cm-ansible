- name : installing packages
  hosts: local
  connection: local
  become: yes

  tasks:
  - name : installing packages
    ansible.builtin.package:
    #   name: "{{ item }}"
    #   state: present
    # loop:
    # - nginx
    # - mysql
    # - curl
    # - git
    # - wget
      name: "{{ name.item }}"
      state: "{{ state.item }}"

    loop:
    - { name : mysql, state : absent }
    - { name : tree, state : present }

