- name: please enter username and password
  hosts: local
  connection: local
  vars_prompt:
  - name: username
    prompt: pls enter u r name
    private: false

  - name: pwd
    prompt: pls enter u r pwd
    private: false

  tasks:
  - name : murali
    ansible.builtin.debug:
      msg: iam {{ name }} {{ pwd }}
