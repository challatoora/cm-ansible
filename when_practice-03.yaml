- name: checking os family
  hosts: local
  connection: local
  become: yes

  tasks:
  - name: checking the os family
    ansible.builtin.pacekge:
      name: httpd
      state: present
    when: ansible_os_family == "RedHat"