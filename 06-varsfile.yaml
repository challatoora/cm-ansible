- name: tast level variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: printing the variables
    vars:
      cource: salesforce
    ansible.builtin.debug:
      msg: "i am {{ peru }}, i am learining {{cource}}"